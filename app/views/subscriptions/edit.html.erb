Please choose your new plan: 
<br>
<%= render :partial => "plans" %>

<br>
<div class="row">
	<div class="col-sm-3 col-sm-offset-3">
		<%= form_tag "/subscriptions" do %>
      <%= collection_select :plan, :stripe_id, @plans, :stripe_id, :name, {}, {class: "form-control"} %>
      <br>
      <input type="submit" class="submit btn btn-primary" value="Update Subscription">
		<% end %>
	</div>
</div>